name: Daily Job

on:
  schedule:
    - cron: '0 16 * * *'

jobs:
    daily-job:
       runs-on: ubuntu-latest

       steps:
         - name: Log daily job message
           run: echo "Daily job done"
       
       